@model JobPortal.ViewModels.EmployerRegisterViewModel

@{
    ViewData["Title"] = "Employee Register";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-lg-6 offset-3">
    <div class="box">
        <h3 class="heading">New account</h3>
        <p class="lead">Not our registered yet?</p>
        <p class="text-muted">
            If you have any questions, please feel free to <a href="#">contact us</a>,
            our customer service center is working for you 24/7.
        </p>
        <form method="post" asp-controller="Account" asp-action="EmployerRegister">
            <div asp-validation-summary="ModelOnly"></div>
            @Html.ValidationSummary()

            <div class="form-group">
                @Html.LabelFor(c => c.FirstName, "Company Name")
                @Html.TextBoxFor(m => m.FirstName, new { @class = "form-control", placeholder = "Company Name" })
            </div>

            <div class="form-group">
                @Html.LabelFor(c => c.LastName, "Company Address")
                @Html.TextBoxFor(m => m.LastName, new { @class = "form-control", placeholder = "Company Address" })
            </div>

            <div class="form-group">
                @Html.LabelFor(c => c.Email, "Email")
                @Html.TextBoxFor(m => m.Email, new { @class = "form-control", placeholder = "email@example.com" })
            </div>

            <div class="form-group">
                @Html.LabelFor(c => c.Password, "Password")
                @Html.TextBoxFor(m => m.Password, new { @class = "form-control", placeholder = "Password", @type = "password" })
            </div>

            <div class="form-group">
                @Html.LabelFor(c => c.ConfirmPassword, "Confirm Password")
                @Html.TextBoxFor(m => m.ConfirmPassword, new { @class = "form-control", placeholder = "Confirm Password", @type = "password" })
            </div>

            @Html.AntiForgeryToken()
            <div class="text-center">
                <button type="submit" class="btn btn-outline-white-primary">
                    <i class="fa fa-user-md"></i> Register
                </button>
            </div>
        </form>
    </div>
</div>